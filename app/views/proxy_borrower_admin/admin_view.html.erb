<!-- Header and Admin Link -->
<span class="flex-heading">
  <span>
    <h1 class="inline-title">All Proxy Borrower Card Requests:</h1>
  </span>
  <span>
    <%= link_to 'Export Data', 
      forms_proxy_borrower_admin_export_path(:format => :csv),
      class: "btn btn-sm btn-primary title-link btn-white-text blue_bg pb-4 mr-3" %>
    <%= link_to 'Admin Home', 
      forms_proxy_borrower_admin_path, 
      class: "title-link text-nowrap" %>
  </span>
</span>
<table class="table table-bordered mt-5">
  <!-- the header row -->
  <thead class="thead-light">
    <tr>
      <th scope="col"><%= sortable "faculty_name", "Faculty/Academic Staff" %></th>
      <th scope="col"><%= sortable "student_name", "DSP Participant" %></th>
      <th scope="col"><%= sortable "research_last", "Proxy (Research Assistant)" %></th>
      <th scope="col"><%= sortable "dsp_rep", "DSP Rep" %></th>
      <th scope="col"><%= sortable "created_at", "Date of Request" %></th>
      <th scope="col"><%= sortable "date_term", "Date proxy expires" %></th>
    </tr>
  </thead>
  <!-- Layout all those beautiful requests -->
  <tbody>
    <% @requests.each do |r| %>
      <tr>
        <td><%= r.faculty_name %></td>
        <td><%= r.student_name %></td>
        <td><%= r.full_name() %></td>
        <td><%= r.dsp_rep %></td>
        <td><%= r.created_at.strftime("%m/%d/%Y") %></td>
        <td><%= r.date_term.strftime("%m/%d/%Y") %></td>
      </tr>
    <% end %>
  </tbody>
</table>
