<h1>MMSID and Tind Information Tool <span style="font-size: 0.5em;">(For hocr collections where one MMSID maps to a single TIND record)</span></h1>
<div style="text-align: right; padding: 10px 10px 40px 10px;"> <%= link_to 'Open Instructions', 'https://docs.google.com/document/d/1wc4PUmbwM2sPShhyOoH1S-_07DcGTLkhK4h1zQKlpcw/edit', title: 'Open the instruction document', target: '_blank' %> </div>
<p>In some collections, one MMSID may correspond to one or multiple TIND records. Even if with one TIND record, it may not be the appending to record. This form will create an MMSID and TIND reocords spreadsheet for users to decide which record to append.</>
<%= form_with(url: '', local: true, target: '_blank') do |f| %>  
  <!-- %= hidden_field_tag :email, @current_user.email % -->
  <hr />
  <p class="required-note">Required</p>
  <div class="form-label form-group required">
    <%= label_tag('directory', 'Resource Directory', class: 'control-label') %>
    <span style="font-size:0.8em">(example: librettos/ucb/incoming or /librettos/ucb/incoming)</span>
  </div>
  <div class="form-input required mb-4">
    <%= f.text_field(:directory, class: 'narrow form-control add-required', aria: { required: true }, required: true, autofocus: true, value: params[:directory]) %>
  </div>
  <div class="form-input">
   <%= f.submit(class: 'btn btn-primary', value: 'Retrieve MMSID-TIND Info') %> 
  </div>
<% end %>
