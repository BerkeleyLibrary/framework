<h1>List of Fees</h1>
<!-- Need descriptive text-->
<% if @fees.fees.length > 0 %>
  <p>Please select the fees you plan to pay. You will be asked to confirm before you are taken to the PayPal payment page. If you have any questions about the listed fees, please contact the <%= link_to 'Privileges Desk', 'https://www.lib.berkeley.edu/find/privileges-desk' %>.</p>
  <!-- Payment Form -->
  <%= form_with url: "/fees/payment", method: :post, local:true do |form| %>
    <%= form.hidden_field :alma_id, :value => @fees.alma_id %>
    <%= form.hidden_field :jwt, :value => @jwt %>
    <!-- rts : responsive table scss -->
    <%= render "fees/fees_table", freeze_pay_column: false %>
    <div class="d-flex justify-content-end pt-2">
      <input type="button" name="reset" class="btn btn-link" value="Select All" onclick="selectAllFees();">
    </div>
    <div class="d-flex justify-content-center pt-3 pb-5">
      <%= form.submit("Confirm Payment", {:class => 'btn btn-primary'}) %>
    </div>
  <% end %>
<% else %>
  <%= render "fees/nonberk_instructions"%>
<% end %>
