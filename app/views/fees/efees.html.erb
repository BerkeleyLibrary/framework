<h1>Non UC Berkeley Patron Fee Payments</h1>
<%= form_with(url: '/efees/lookup', method: :get, local: true) do |f| %>
<p>Enter the patrons Alma ID below in order to lookup the patron's fees. You can then send an email to the patron with a link with instructions to pay fees online.</p>
<%# Primary ID %>
<div class="form-label form-group required">
  <%= label_tag(:alma_id, 'Alma ID:', class: 'control-label') %>
</div>
<div class="form-input required mb-4">
  <%= f.text_field(:alma_id, :onkeyup => 'checkFormInputs()', class: 'narrow form-control add-required', required: true, autofocus: true) %>
</div>
<div class="form-input">
  <p><input type="submit" id='submit-btn' class='btn btn-primary' value="Look Up Fees"></p>
</div>
<% end %>