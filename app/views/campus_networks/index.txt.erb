University of California, Berkeley
<%= page_header %>
Updated <%= Time.now.strftime('%d %b %Y') %>


VERSION 1 (comma delimited)
============================================
<%= networks.select(&:ipv4?).map(&:to_vendor_star_format).join(', ') %>


VERSION 2 (same as above, but not comma delimited)
============================================
<%= networks.select(&:ipv4?).map(&:to_vendor_star_format).join("\n") %>


VERSION 3 (the format preferred by some publishers)
============================================
<%= networks.select(&:ipv4?).map(&:to_vendor_range_format).join("\n") %>

Each "*" means the range from 0 to 255

IP Version 6
============================================
<%= networks.select(&:ipv6?).map(&:to_vendor_range_format).join("\n") %>
