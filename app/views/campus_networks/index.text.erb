University of California, Berkeley
<%= page_header %>
Updated <%= Time.now.in_time_zone("Pacific Time (US & Canada)").strftime('%I:%M:%S %p %Z %b %d %Y')  %>
<% formatted = get_formatted_ranges(networks) %>


VERSION 1 (comma delimited)
============================================
<%= formatted[:starred].join(", ") %>

VERSION 2 (same as above, but not comma delimited)
============================================
<%= formatted[:starred].join("\n") %>


VERSION 3 (the format preferred by some publishers)
============================================
<%= formatted[:ranged].join("\n") %>

Each "*" means the range from 0 to 255


IP Version 6
============================================
<%= @generated.sort.join("\n") %>
